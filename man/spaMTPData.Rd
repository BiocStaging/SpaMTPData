% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resources.R
\name{spaMTPData}
\alias{spaMTPData}
\title{Retrieve a SpaMTP experiment resource}
\usage{
spaMTPData(
  resource,
  version = "latest",
  local_dir = NULL,
  hub = NULL,
  metadata = FALSE,
  offline = FALSE,
  fallback_url = TRUE,
  cache_dir = NULL,
  timeout = 1800,
  retries = 3L,
  verify = TRUE
)
}
\arguments{
\item{resource}{Resource name; see \code{\link[=spaMTPDataResources]{spaMTPDataResources()}}.}

\item{version}{Data release version or \code{"latest"}.}

\item{local_dir}{Optional directory containing downloaded source files.}

\item{hub}{Optional pre-created \code{ExperimentHub} object.}

\item{metadata}{Return only the registry row.}

\item{offline}{If \code{TRUE}, use local files or a verified cache only; never
query ExperimentHub or the source URL.}

\item{fallback_url}{If \code{TRUE}, use the immutable source URL when the resource
has not yet been ingested into ExperimentHub.}

\item{cache_dir}{Cache directory for source-URL downloads. Defaults to the
platform-specific user cache returned by \code{\link[tools:R_user_dir]{tools::R_user_dir()}}.}

\item{timeout}{Download timeout in seconds for the source-URL fallback.}

\item{retries}{Number of verified download attempts.}

\item{verify}{Verify local files against the published size and checksum.
Set to \code{FALSE} only for intentional development fixtures. Downloaded and
cached files are always verified; loaded object classes are always checked.}
}
\value{
The requested experiment object or local file path. With
\code{metadata = TRUE}, returns one registry row.
}
\description{
Returns the representation actually stored in the published file; no
implicit conversion is performed. Release \verb{1.1.0}, selected by default,
supplies seven native SpatialExperiment datasets. Use \code{version = "1.0.0"}
to request a historical release explicitly. Registry entries with
\code{requires_conversion = TRUE} are historical Seurat objects. Convert these
explicitly at the SpaMTP workflow boundary, or use \code{\link[=spaMTPExampleData]{spaMTPExampleData()}} for
a small native example that needs neither Seurat nor network access.
Local files and a verified download cache are checked before any Hub query.
Existing immutable files retain their original classes and checksums.
Loaded native experiments additionally carry \code{metadata(x)$SpaMTPData}
with registry provenance and species, enabling analysis packages to check
annotation compatibility. Assays, row names and coordinates are preserved.
Provenance is also carried by nested \code{altExp()} experiments so that species
checks survive extracting a paired transcriptome. Explicit species metadata
already declared by a child experiment is retained rather than relabelled.
}
\examples{
spaMTPData("mouse_brain_dhb_striatum", metadata = TRUE)
}
